import { add } from "./add";
import { multiply } from "./multiply";
import { sub } from "./sub";

export const sum = add(1, 1);
export const minus = sub(10, 5);
export const product = multiply(2, 2);
